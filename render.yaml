services:
  - type: web
    name: nba-lineup-optimizer
    runtime: python
    buildCommand: pip install -r requirements.txt && python backend/scrapers/pipeline.py
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
